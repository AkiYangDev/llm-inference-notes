# 贡献与维护

## 文件组织

文章使用英文小写短横线命名，例如 `request-lifecycle.md`。选择最主要的主题目录存放，通过相对链接关联其他主题，避免复制同一篇文章。

新增文章时更新所在目录的 `README.md`。配图放在 `assets/<article-slug>/`，配套示例放在 `examples/<example-slug>/`。目录只收录实际存在的文件。

## 内容要求

源码文章注明仓库、commit 或 release、关键配置和适用范围；部署记录注明硬件与软件版本、验证命令及实际结果。把原理说明、源码确认和实测结果分清楚，性能数据同时给出测量条件。

引用尽量指向官方文档、论文或固定版本源码。公开前检查是否包含凭据、内部地址、未公开模型和工作环境信息。

## 提交与检查

在仓库根目录运行：

```bash
python3 scripts/check_docs.py
```

检查器使用 Python 标准库，检查 Markdown 编码、换行、冲突标记与普通内联相对文件链接。它不验证外链、标题锚点、引用式链接或技术结论，这些需要人工核对。

提交说明描述具体变化。PR 中说明内容范围、证据版本和验证结果。文档正文保持连贯，模板中的提示在发布前删除。
